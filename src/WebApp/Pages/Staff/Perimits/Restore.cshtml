@page "{id:Guid?}"
@using SWGW.WebApp.Pages.Shared.EditorTemplates
@model RestoreModel

@{
    ViewData["Title"] = $"Restore Permit ID {Model.PermitDto.PermitId}";
    ViewData["NoAnchors"] = "True";
}

<h1>
    <em>Confirm</em> @ViewData["Title"]
</h1>

<div class="p-3 border rounded-3 bg-dark-subtle text-dark-emphasis">
<div class="p-3 border rounded-3 bg-dark-subtle text-dark-emphasis">
    <p class="lead text-warning-emphasis font-weight-medium">
        Are you sure you want to restore this deleted Permit?
    </p>

    <partial name="Shared/_PermitSummaryPartial" model="Model.PermitView" />

    <form method="post">
        <div class="mb-3">
            @Html.EditorFor(model => model.PermitDto.Comments, EditorTemplate.Textarea, new { Rows = 3 })
        </div>
        <div class="mt-3 mb-1">
            <button type="submit" class="btn btn-warning col-5 col-sm-3 col-lg-2 me-2">Restore</button>
            <a asp-page="Details" asp-route-id="@Model.PermitDto.PermitId.ToString()" class="btn btn-outline-secondary">Cancel</a>
        </div>
        <input asp-for="PermitDto.PermitId" type="hidden" />
    </form>
</div>
